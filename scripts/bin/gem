#!/bin/bash
# Lazy wrapper for gem

# Remove ourselves from PATH temporarily to avoid recursion
WRAPPER_DIR="$(dirname "$(readlink -f "$0")")"
export PATH="${PATH//$WRAPPER_DIR:/}"

# Load rbenv which sets up gem in PATH
. "$WRAPPER_DIR/rbenvload" &> /dev/null

# Now call the real gem
exec gem "$@"
